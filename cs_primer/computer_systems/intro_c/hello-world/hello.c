#include <stdio.h> // include the standard input/output library

int main(void) { // int main() is also valid, void just means no arguments
    printf("Hello, World\n");  // \n is a newline character
    return 0; // return 0 means the program ran successfully
}